# PROG-20260723

## 今日完成

- 对 `todo.md` 中 3 条历史优化项做代码库现状定位。
- 确认前端 npm 构建失败日志仍会展示给用户，尚未完成隐藏或脱敏。
- 确认首轮生成已改为 `/app/chat` 智能路由，但尚未支持“纯问答/无生成意图”跳过 App 创建和构建。
- 确认 App 名称仍默认取 `initPrompt` 前 12 位，尚未完成更合理的初始化策略。

## 验证证据

- 读取 `docs/文档治理规范.md`、`docs/项目总览.md`、`docs/后端架构与开发规范.md`、`docs/前端架构与开发规范.md`、`docs/AI生成链路与SSE协议.md`、`docs/前端对接说明-Vue工程化生成.md`。
- 检查 `prompt-ship-frontend/src/pages/AppChatPage.vue`、`prompt-ship-frontend/src/api/services/appService.ts`、`src/main/java/com/xiaoyu/promptship/service/impl/AppServiceImpl.java`、`src/main/java/com/xiaoyu/promptship/core/vue/VueProjectBuilder.java`、`src/main/resources/template/system-prompt-router.txt`。

## 遇到的问题

- PowerShell 默认输出中文出现乱码，已用 UTF-8 重新读取确认文件内容。
- 工作区已有未提交改动：`README.md`、`src/main/java/com/xiaoyu/promptship/service/impl/AppServiceImpl.java`，本次只读取不覆盖。

## 遗留问题

- ~~待决定是否将这 3 条 todo 拆成正式 `REQ / DESIGN / BUG` 后再修复。~~ → 已决定不拆，直接修复。
- ~~待决定”纯问答”是否应进入独立聊天链路~~ → 已决定不增加非生成路由类型。

- **todo-1 前端隐藏 npm 构建失败日志** `[BUG-20260723-01](../bugs/BUG-20260723-01-npm构建失败日志暴露.md)`：删除 `AppChatPage.vue` 中 `<pre class=”build-log”>` 块，失败文案改为用户友好提示，不再暴露 npm 日志原文。后端 `VueProjectBuilder` 日志保留供排查。type-check + build 通过。
- **todo-2 优化 App 名称初始化** `[DESIGN-20260723-01](../designs/DESIGN-20260723-01-App名称初始化优化.md)`：新增 `AppServiceImpl.generateDefaultAppName()` 私有方法，链路为 取首句 → 去前缀动词 → 疑问句转陈述 → 截断 18 字 → 兜底”未命名应用”。替换 `createApp` 中原有的 `subPre(initPrompt, 12)` 硬截断逻辑。编译通过。
- **截图逻辑迁移**：封面截图从 `deployApp()` 移至代码生成完成后（`bridgeVueTokenStream.onCompleteResponse` + `bridgeFluxToSse.doOnComplete`），使用 `code_output` 预览地址替代 `deployKey` 地址，旧端点 codeGenType null 暂从 DB 回退。编译通过。

## 下一步

- todo-3 “纯问答不触发生成” 暂缓，当前决策为不引入非生成路由类型。
